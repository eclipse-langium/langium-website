{
  "name": "langium-website",
  "version": "0.0.0",
  "description": "Source code for langium.org",
  "private": true,
  "scripts": {
    "start": "concurrently \"NODE_ENV=development tailwindcss -i ./src/style.css -o ./public/css/style.css --watch\" \"live-server public\"",
    "build": "NODE_ENV=production tailwindcss -i ./src/style.css -o ./public/css/style.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "TypeFox",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^6.2.2",
    "live-server": "^1.1.0",
    "tailwindcss": "^2.2.16"
  }
}
