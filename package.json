{
  "name": "langium-website",
  "version": "0.0.0",
  "description": "Source code for langium.org",
  "author": "TypeFox",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "concurrently \"NODE_ENV=development tailwindcss -i ./documentation/tailwind/style.css -o ./documentation/static/css/style.css --watch\" \"hugo server -D -b `gp url 1313` -s documentation --appendPort=false\"",
    "build": "NODE_ENV=production tailwindcss -i ./documentation/tailwind/style.css -o ./documentation/static/css/style.css --minify && hugo --gc --minify -b / -s documentation",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^6.2.2",
    "hugo-extended": "^0.89.4",
    "live-server": "^1.1.0",
    "tailwindcss": "^2.2.16"
  }
}
